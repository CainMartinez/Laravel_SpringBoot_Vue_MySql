
const state = {
    // Aqui van las cariables
};

const mutations = {
    // Aqui van las funciones que modifican las variables
};

const actions = {
    // Aqui van las funciones que se comunican con el backend
};

const getters = {
    // Aqui van las funciones que devuelven las variables
};

export default {
    namespaced: true,
    state,
    mutations,
    actions,
    getters,
};